import React from "react";
import { render } from "react-dom";
import styled from "styled-components";

const Wrapper = styled.div`
  font-family: sans-serif;
  font-size: 20px;
`;

export default () => (
  <Wrapper>
    まったこの中身って結構な長文書かなきゃいけないやつかもしかして
  </Wrapper>
);
